% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summarize_shap_rslt}
\alias{summarize_shap_rslt}
\title{Summarize SHAP results from run_survey_shap()}
\usage{
summarize_shap_rslt(
  shap_rslt,
  top_n = 5L,
  min_n_main = 50L,
  min_n_interaction = 100L,
  quiet = FALSE
)
}
\arguments{
\item{shap_rslt}{A list returned by \code{run_survey_shap()}.}

\item{top_n}{Number of top and bottom rows to return for direction tables.}

\item{min_n_main}{Minimum per-level sample size (\code{n}) for one-way direction results.}

\item{min_n_interaction}{Minimum \code{n_active} for two-way direction results.}

\item{quiet}{If TRUE, do not print; only return the summary object.}
}
\value{
An object of class \code{surveySHAP_summary} (a named list) containing:
\describe{
\item{strength_main_group}{Group-level one-way strength table.}
\item{strength_interaction_group}{Group-level two-way strength table.}
\item{direction_main_top}{Top \code{top_n} one-way directional rows (after filtering).}
\item{direction_main_bottom}{Bottom \code{top_n} one-way directional rows (after filtering).}
\item{direction_interaction_top}{Top \code{top_n} interaction directional rows (after filtering).}
\item{direction_interaction_bottom}{Bottom \code{top_n} interaction directional rows (after filtering).}
\item{filters}{A list describing how many rows were removed by sample-size filters.}
}
}
\description{
Produces compact summaries for the main outputs of \code{run_survey_shap()}:
group-level strength (one-way and two-way) and directional effects
(top/bottom levels and pairs).
}
\details{
Direction tables are sorted by signed SHAP values and truncated to
top/bottom \code{top_n}. Two-way direction results are filtered by
\code{min_n_interaction} to avoid unstable estimates from rare co-occurrences.
}
