% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_tables.R
\name{summarize_interaction_shap}
\alias{summarize_interaction_shap}
\title{Summarize interaction SHAP into strength and direction tables}
\usage{
summarize_interaction_shap(
  interaction_obj,
  design,
  model = c("lm", "glm", "xgb_numeric", "xgb_logistic"),
  w = NULL,
  min_n_eff = 100
)
}
\arguments{
\item{interaction_obj}{Interaction SHAP representation. For linear models,
this is an \eqn{n \times q} matrix with one column per level pair. For
xgboost, this is an \eqn{n \times p \times p} array over the main one-hot
features only.}

\item{design}{A design object from \code{\link[=build_survey_design]{build_survey_design()}}.}

\item{model}{One of the four supported model labels.}

\item{w}{Optional weights.}

\item{min_n_eff}{Minimum effective sample size for direction rows.}
}
\value{
A list with \code{strength} and \code{direction} data.frames.
}
\description{
Both strength and direction are computed from grouped feature-pair SHAP
contributions. For a pair of categorical variables, SHAP interaction values
are first summed over all one-hot level-pair columns belonging to that
feature pair; strength then averages the absolute grouped contribution,
while direction averages the signed grouped contribution conditional on each
observed level pair.
}
